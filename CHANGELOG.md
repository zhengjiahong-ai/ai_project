# 更新日志 (CHANGELOG)

本记录用于追踪学术 AI 助手的功能迭代与优化。

### 2026-04-10
1. **全景翻译升级为纯译文页**：将右侧翻译页从纯翻译模式，调整到“原页底图 + 中文覆盖”的高保真模式，调整为“纯中文译文页”展示；不再回放整页英文 PDF 背景，也移除了原文预览卡片，右侧仅保留译文结果、必要的加载态/错误态与重试入口。
2. **结构化译文排版重做**：前端新增可读优先的翻译布局逻辑，使用结构化 `translatedBlocks` 按标题区、作者区、正文区和双栏顺序重新排版，不再依赖固定高度绝对定位覆盖，重点修复标题页、双栏正文页和长段落页中的文本重叠与挤压问题；当结构化布局不足时，会自动降级为连续纯文本译文。
3. **新增图表/表格前端裁图保留能力**：基于现有 `translationLayoutIndex.excludedZones` 与页面快照，在前端提取当前页时裁出图片、图表、表格等保留区域，生成 `figureSnippets` 并在右侧翻译页按阅读顺序渲染，从而在不显示英文正文的前提下保留原图内容。
4. **补齐全景翻译状态兼容链路**：扩展 `translationState` 的页级结构，兼容旧版仅含 `translatedText` 的缓存记录，并支持新版 `translatedBlocks / figureSnippets / pageLayout / renderMode` 数据；同时打通翻页、重试、刷新恢复缓存和切换论文时的翻译页状态同步，避免图表保留能力只存在于当前内存态。
5. **新增全局夜间模式**：前端引入 `light / dark` 两个主题模式，在导航栏新增手动切换入口，并按 `localStorage['pixiu-theme']` 持久化用户选择，刷新页面后仍会保持当前主题。
6. **统一右侧功能面板的暗色风格**：基于 CSS 变量重新梳理聊天、篇章解构、批判分析、引导式学习、全景翻译、学术笔记和论文库侧边栏的卡片、输入框、按钮、弹层、滚动条和 `Markdown / KaTeX` 配色，在夜间模式下提升整体可读性和视觉一致性。
7. **增强 PDF 阅读器的夜间阅读能力**：将 `react-pdf-viewer` 从固定 `light` 主题改为跟随全局主题，并在暗色模式下对 PDF `canvas` 应用反相/亮度/对比度滤镜，使左侧 PDF 区域不仅外壳变暗，文档内容也能以更适合夜间阅读的方式显示。

### 2026-04-09
1. **修复 PDF 划词公式在用户消息中的展示内容**：调整划词解释链路，用户气泡中只保留选中的原文或归一化后的公式本体，不再显示“请解释以下内容”“归一化后的选区”“原始选区”等额外说明文字。
2. **收紧公式识别与归一化策略**：优化 `pdfFormulaSelection` 与前端数学预处理逻辑，降低普通英文说明文字被误判为公式的概率，并补强对残缺 PDF 数学片段的归一化恢复能力。
3. **改进聊天气泡中的数学公式渲染**：将用户侧独立公式消息切换为展示公式模式，调整聊天气泡样式与 KaTeX 相关 CSS，减少上下标、集合符号和长公式在右侧紫色消息气泡中的排版异常。
4. **去除 AI 回答中的恢复过程铺垫**：更新公式解释请求提示词，要求模型直接执行解释或翻译任务，不再输出“根据你提供的原始选区内容”“恢复后的公式如下”等恢复过程说明。
5. **修复 PDF 划词浮层文案异常**：恢复并统一 PDF 划词浮动按钮与解释弹窗中的可见中文文案，修正此前改动引入的乱码和错误标签显示问题。

### 2026-04-05
1. **重构引导式学习链路**：将原本“生成问题后由 AI 自答”的伪苏格拉底流程，改为“AI 提问、用户作答、AI 评估并继续追问”的真实引导式学习模式，共 5 轮并在末尾输出总结。
2. **新增会话式引导学习接口**：Python 新增 `/api/socratic-session/start` 与 `/api/socratic-session/answer` 两个接口及对应请求模型，Java 同步补齐透传路由，前端改为调用新接口而不再复用普通聊天接口。
3. **支持引导学习进度恢复**：前端将引导学习状态单独持久化到 IndexedDB `sessionStore`，按论文隔离保存阅读进度、当前题目、用户回答、掌握度评估与最终总结，刷新页面或切换论文后可恢复。
4. **升级引导式学习界面**：右侧面板改为“学习引导 + 已完成题卡 + 当前作答卡 + 未解锁题卡 + 总结”结构，展示用户回答、掌握度、AI 简评和下一步提示，并提供“重新开始”入口。
5. **新增全景翻译真实链路**：将原本仅切换视觉占位层的“全景翻译”按钮升级为真实功能，前端基于当前 PDF 页提取文本并在右侧展示逐页中文译文，Java 与 Python 同步新增 `/api/translate-page` 接口，译文结果按论文与页码缓存到 IndexedDB `translationStore`，且保留原有聊天、划词解释、批判阅读、引导式学习与笔记功能不受影响。
6. **数学公式渲染与 PDF 选区公式归一化补全**：补齐 AI 对话、划词解释、批判分析、篇章解构、引导式学习和学术笔记中的数学公式显示链路，在原有 KaTeX 渲染基础上继续兼容 `\(...\)` 与 `\[...\]` 这类模型常见输出；同时新增 PDF 选区公式归一化逻辑，将复制后丢失上下标的扁平文本优先恢复为可渲染的 Markdown LaTeX，再配合“先恢复公式再解释”的提示词发送给 AI，提升用户消息、AI 回复和 PDF 划词场景下数学符号的正确显示率。

### 2026-04-04
1. **打通批判性阅读真实链路**：前端移除 `mockResult`，改为调用 Java `POST /api/critical-reading/{pdfId}`；Java 再转发到 Python `/api/deep-analysis`；Python 支持通过 `pdf_id` 从 RAG 分块反查论文内容后再输出真实分析结果。
2. **补齐聊天历史接口**：Java 新增 `GET /api/chat/history/{sessionId}`，按时间顺序从 H2 返回结构化消息；前端在恢复会话和切换论文时会调用该接口并与 IndexedDB 缓存同步。
3. **拆分 Python 服务结构**：将原本臃肿的 `ai-service-python/main.py` 拆分为 `routes / services / llm / rag / schemas` 模块，同时保留 `main.py` 入口与现有 `/api/*` 路径兼容。
4. **补基础测试并清理旧文件**：新增前端 API smoke test、Java controller/service 测试、Python 路由测试；清理未引用的 `GuidedLearningPanel.jsx`、`usePdfFile.js`、前端模板 README、旧测试脚本和未使用 Python 模块。
5. **修复引导式学习逻辑**：将原本的“问题一”删除，最后一个问题改为结论，同时删除两个不必要的“回答此问题”按钮。

### 2026-04-03
1. **AI 响应提速**：将底层向量模型更换为 130MB 轻量版，彻底解决了首次提问时的长延时和卡顿问题。
2. **加载机制优化**：新增模型持久化卷，模型仅需在首次启动时下载一次，后续开机即用。
3. **界面清爽化**：移除了 AI 消息气泡中的错位角标，删除了底部的冗余标签，对话视野更开阔。
4. **使用手册补全**：新增《启动服务.md》，详细说明了 Docker 部署步骤及日志查看方法。

### 2026-04-02
1. **多论文库支持**：新增侧边栏“图书馆”功能，支持多份 PDF 文件的管理、切换与独立删除。
2. **数据存储升级**：使用浏览器本地存储 (IndexedDB) 隔离不同论文的对话和笔记，互不干扰。
3. **划词高亮持久化**：所有的划线解释记录会自动保存在 PDF 上，刷新或切换论文后依然存在。
4. **学术笔记收藏**：在对话气泡旁增加收藏按钮，支持一键将 Q&A 存入学术笔记库。
5. **异常反馈增强**：实现了 AI 回答的中止功能，并会在对话流中实时留下“回答已取消”的 Markdown 提示。

### 2026-04-01
1. **全栈架构打通**：完成 Docker 容器化部署，整合 React 前端、Java 后端、Python AI 接口及 Grobid 解析引擎。
2. **智能解构功能**：实现 PDF 自动提取摘要、方法、结果等核心章节，并自动生成篇章总结。
3. **开发环境体验**：开启 Python 后端热重载，代码改动后实时生效，无需重新构建容器。
4. **基础交互构建**：支持 Markdown 格式渲染、对话删除、PDF 划词实时解释等基础学术分析功能。
