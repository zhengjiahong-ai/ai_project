# 🚀 项目启动与运行指南

本项目采用 Docker 容器化部署，集成了前端 (React)、Java 后端、Python AI 服务及 Grobid 文献解析引擎。

## 🛠️ 启动前的准备
确保你的电脑已安装：
- **Docker Desktop** (且处于运行状态)
- **Docker Compose**

---

## 🏗️ 快速启动步骤

### 1. 启动所有服务
在项目根目录下打开终端，执行以下命令：
```bash
docker-compose up -d
```
> `-d` 表示在后台运行。首次运行时，Docker 会自动拉取镜像并构建容器，这可能需要几分钟。

### 2. 检查 AI 服务初始化（关键！）
由于 AI 引擎需要加载向量模型（BGE-Small），**首次启动**会有一次约 130MB 的模型下载过程。

请运行以下命令查看 AI 服务的实时进度：
```bash
docker-compose logs -f ai-service
```

**看到以下消息即表示启动成功：**
- `INFO: Application startup complete.`

### 3. 访问项目
一旦上述日志显示完成，即可通过浏览器访问：
- **前端页面**: [http://localhost:5173](http://localhost:5173)

---

## 💡 重要注意事项

- **首次下载偏慢**：模型文件存储在海外服务器，首次下载可能需要 1-3 分钟（取决于网速）。
- **持久化缓存**：我们已经配置了 `huggingface_cache` 持久化卷。**模型只需下载一次**，之后无论是重启电脑还是重启容器，AI 都能秒速就绪。
- **内存建议**：本项目涉及多个深度学习模型运行，建议为 Docker Desktop 分配至少 **4GB** 内存以保证运行流畅。

---

## ❓ 常见问题处理

### Q: 提问时 AI 一直显示“正在思考”且没有回复？
- **原因**：通常是因为后台模型还在下载中。
- **解决方法**：使用 `docker-compose logs -f ai-service` 查看日志。如果下载卡住了，可以尝试运行 `docker-compose restart ai-service` 强制刷新。

### Q: 如何彻底关闭所有服务？
- 运行：`docker-compose down`

### Q: 修改了代码后需要重启吗？
- 前端通常不需要手动重启，已配置 **代码热更新 (Hot Reload)**。Python AI 服务为避免文件监听器扫描临时目录导致崩溃，默认不启用 `--reload`；修改 Python 代码后请重建对应容器。
- 如果你修改了 **Java 后端**（`backend-java`）代码，例如新增接口或改动 Controller / Service，需要重新构建并启动容器：
```bash
docker-compose up -d --build backend
```
- 如果同时修改了 Python AI 服务，也建议一起重建对应容器，避免前后端接口版本不一致：
```bash
docker-compose up -d --build backend ai-service
```
