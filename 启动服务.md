# Pixiu 启动服务

## Docker Compose

在项目根目录启动主要服务：

```bash
docker compose up --build
```

访问前端：`http://localhost:5173`。可选 Neo4j 使用：

```bash
docker compose --profile neo4j up -d
```

## 本地分服务

Python AI 服务：

```bash
cd ai-service-python
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```

Java 网关和前端分别在 `backend-java`、`frontend` 目录使用仓库 README 中的命令启动。GROBID 建议通过 Docker Compose 启动。

## 可选只读 MCP adapter

MCP adapter 不是 FastAPI 路由或 Docker Compose 常驻服务，默认关闭。由本机 MCP 客户端单独启动时配置：

```json
{
  "command": "python",
  "args": ["-m", "mcp_adapter"],
  "cwd": "C:/path/to/ai_project/ai-service-python",
  "env": {
    "PIXIU_MCP_ENABLED": "true"
  }
}
```

未设置开关时命令会以非零状态退出。该入口仅允许三个有界只读工具，不提供 HTTP/SSE、文件系统、全文导出、API key、prompt 或 trace 访问；不得转发为公网服务。
