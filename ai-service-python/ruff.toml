# Ruff configuration for Pixiu AI service
# Pre-existing refactoring issues (v0.6.15-v0.6.22) are documented below.
# These per-file-ignores should be removed as each file is properly fixed.

[lint]
[lint.per-file-ignores]
# ── Pre-existing refactoring bugs ────────────────────────────────────────────
# chat_service.py: functions were moved to socratic_service/term_explanation_service
# during v0.6.15 split; internal call sites were not updated.
"services/chat_service.py" = ["F821", "E402"]
# socratic_service.py: SOCRATIC_* constants need to be imported from shared module
"services/socratic_service.py" = ["F821"]
# term_explanation_service.py: missing imports from chat_service split (v0.6.15)
"services/term_explanation_service.py" = ["F821"]
# research_task_service.py: _detect_research_conflicts redefined (aggregator vs conflict)
"services/research_task_service.py" = ["F811", "F821"]
# evidence_cross_validator.py: _cluster_and_score reference (v0.6.22)
"services/evidence_cross_validator.py" = ["F821"]
# reasoning_chain.py: _summarize_chain reference
"services/reasoning_chain.py" = ["F821"]
# agent_reasoning.py: Optional type annotation
"services/agent_reasoning.py" = ["F821"]

# ── Intentional E402 (logger before service imports) ─────────────────────────
"services/agent_orchestrator.py" = ["E402"]
"services/analysis_service.py" = ["E402"]
"services/page_translation_service.py" = ["E402"]
"services/query_service.py" = ["E402", "F841"]
"services/research_planner.py" = ["E402"]
"services/retrieval_judge_service.py" = ["E402"]

# ── Known unused variables (readability / future use) ────────────────────────
"services/adversarial_reviewer.py" = ["F841"]
"services/agent_project_service.py" = ["F841"]
"services/background_knowledge_service.py" = ["F841"]
"services/research_session.py" = ["F841"]

# ── Ambiguous variable name ──────────────────────────────────────────────────
"services/cross_lingual.py" = ["E741"]
