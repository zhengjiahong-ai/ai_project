name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── Frontend ──────────────────────────────────────────────────────────────
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    env:
      NODE_OPTIONS: --experimental-strip-types
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci --legacy-peer-deps || npm install --legacy-peer-deps
      - run: npm run lint
      - run: npm run typecheck

  # ── Frontend E2E ───────────────────────────────────────────────────────────
  e2e:
    needs: frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci --legacy-peer-deps || npm install --legacy-peer-deps
      - run: npx playwright install chromium --with-deps
      - run: npm run test:e2e
        env:
          CI: true
      - run: npm test

  # ── Python AI Service ─────────────────────────────────────────────────────
  python:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-service-python
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: ai-service-python/requirements.txt
      - run: pip install -r requirements.txt
      - run: pip install ruff
      - run: ruff check services/ routes/ core/
      - run: PYTHONPATH=. python -m pytest tests/ --timeout=60 --ignore=tests/test_code_execution_api.py --ignore=tests/test_offline_llm.py --ignore=tests/test_offline_core_paths.py --ignore=tests/test_external_query_planner.py --ignore=tests/test_logging_config.py --ignore=tests/test_council_service.py --ignore=tests/test_health.py --ignore=tests/test_research_task_service.py --ignore=tests/test_citation_responses.py --ignore=tests/test_api_contract_smoke.py

  # ── Java Backend ──────────────────────────────────────────────────────────
  java:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend-java
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: temurin
          cache: maven
      - run: mvn -B verify -DskipTests
      - run: mvn -B test
